// Diffrence between innerHTML , innerText , innerContent
//HTMLCollection(no looping fxn),NodeList(only forEach) and Array they are similar but not same-->They  can be converted into array using Array.from(--);

// const allH2=document.querySelectorAll('h2')
// allH2.forEach((li)=>{
//     li.style.color="pink";
//      li.style.backgroundColor="blue";
//    li.innerHTML="<h1>Rohit</h1>"
// })